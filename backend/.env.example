DB_URL=jdbc:postgresql://<host>/<database>?sslmode=require&channel_binding=require
DB_USERNAME=<username>
DB_PASSWORD=<password>
JWT_SECRET=<generate a random 256-bit+ secret, e.g. `openssl rand -base64 64`>
